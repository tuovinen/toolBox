A script to separate customer specific sheets (service reports) from Excel file. Also validates for example some cell values and creates a filename from a cell value which to save the file. Each sheet is saved as a file which then can be sent to customers.