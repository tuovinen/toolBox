This tool is used to test HTTP(S) requests and/or get an IP address of the source host. The application runs on Heroku cloud server and is written in Python (Flask).

URL to test: https://pytest101.herokuapp.com?param1=true